def test_ci_wiring_smoke() -> None:
    """Гарантирует хотя бы один собранный тест.

    Если все 10 задач ещё не решены, все эталонные тесты пропускаются
    (skip) на этапе сбора, и pytest видит 0 собранных тестов — это код
    выхода 5 ("no tests collected"), то есть red pipeline на пустом
    шаблоне. Этот тест всегда собирается и проходит, поэтому пайплайн
    остаётся зелёным до тех пор, пока решения реально не ломают тесты.
    """
    assert True
