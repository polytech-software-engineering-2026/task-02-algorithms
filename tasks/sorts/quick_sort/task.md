## Описание задачи
Реализуйте быструю сортировку для массива целых чисел по возрастанию.

## Требования
- Написать функцию быстрой сортировки
- Сортировка по возрастанию
- Допускается использовать вспомогательные функции

## Ограничения
- 0 ≤ n ≤ 10^5
- |a[i]| ≤ 10^9
- Асимптотика: средняя O(n log n), худшая O(n^2); память — глубина стека рекурсии O(log n) в среднем

## Формат ввода
- Параметр функции: `arr: list[int]`

## Формат вывода
- Функция должна вернуть новый отсортированный массив

## Примеры
```
Вход:
[3, 7, 9, 4, 3, 1, 8, 5]

Выход:
[1, 3, 3, 4, 5, 7, 8, 9]
```

```
Вход:
[]

Выход:
[]
```

## Тесты
- Опишите от 5 тест кейсов, проверяющих корректность и алгоритмическую сложность предложенного решения

```python
def quick_sort(arr: list[int]) -> list[int]:
    ...


# пример теста
def test():
    assert quick_sort([]) == []
    assert quick_sort([3]) == [3]
    assert quick_sort([3, 7, 9, 4, 3, 1, 8, 5]) == [1, 3, 3, 4, 5, 7, 8, 9]
    assert quick_sort([5, 4, 3, 2, 1]) == [1, 2, 3, 4, 5]
    assert quick_sort([2, -1, 2, -1, 0]) == [-1, -1, 0, 2, 2]
```
